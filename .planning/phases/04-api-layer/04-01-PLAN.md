---
phase: 04-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/cache/redis.ts, src/lib/cache/cache.ts]
autonomous: true
user_setup:
  - service: redis
    why: "Cache hot analytics queries with 5-minute TTL"
    env_vars:
      - name: REDIS_URL
        source: "Redis provider dashboard or local redis connection string"

must_haves:
  truths:
    - "API cache can read/write JSON in Redis with a 5-minute TTL"
    - "Redis client is singleton with error listener to avoid connection storms"
  artifacts:
    - path: "src/lib/cache/redis.ts"
      provides: "Singleton Redis client using REDIS_URL"
      exports: ["getRedisClient"]
    - path: "src/lib/cache/cache.ts"
      provides: "Cache helpers for JSON get/set with default TTL"
      exports: ["getCachedJson", "setCachedJson", "withCache"]
  key_links:
    - from: "src/lib/cache/redis.ts"
      to: "process.env.REDIS_URL"
      via: "connection url lookup"
      pattern: "REDIS_URL"
    - from: "src/lib/cache/cache.ts"
      to: "src/lib/cache/redis.ts"
      via: "client getter"
      pattern: "getRedisClient"
---

<objective>
Create a Redis cache layer for API endpoints.

Purpose: Ensure hot analytics queries are cached with a 5-minute TTL.
Output: Redis client singleton and JSON cache helpers.
</objective>

<execution_context>
@/home/guichu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/guichu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-layer/04-RESEARCH.md
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Redis client singleton</name>
  <files>src/lib/cache/redis.ts</files>
  <action>
Create a Redis client module using node-redis `createClient` with an `error` listener before `connect()`. Read the connection string from `process.env.REDIS_URL`, avoid per-request clients by caching the connected client in module scope, and return `null` with a warning when the env var is missing so API handlers can bypass caching in dev. Export a `getRedisClient()` async helper that lazily connects once and reuses the same instance.
  </action>
  <verify>pnpm exec tsc --noEmit</verify>
  <done>Redis client module exists, reuses a single connection, and guards missing REDIS_URL without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Add cache-aside JSON helpers</name>
  <files>src/lib/cache/cache.ts</files>
  <action>
Implement `getCachedJson<T>(key)` and `setCachedJson(key, value, ttlSeconds = 300)` that use the Redis client to `GET`/`SET` JSON values with `{ EX: ttlSeconds }`. Add `withCache<T>(key, ttlSeconds, fetcher)` to wrap cache-aside behavior: return cached JSON on hit, otherwise call `fetcher`, store JSON, and return. Ensure parse errors are handled safely (treat as miss) and no caching occurs when the client is null.
  </action>
  <verify>pnpm exec tsc --noEmit</verify>
  <done>Cache helpers can safely read/write JSON with a default 5-minute TTL and tolerate missing Redis.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit`
</verification>

<success_criteria>
- Redis singleton and cache helper modules are available for API route handlers.
- Cache helper uses a 300-second default TTL and never creates per-request clients.
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-layer/04-01-SUMMARY.md`
</output>
