---
phase: 05-dashboard-frontend
plan: 04
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/app/dashboard/_components/RankingsTable.tsx, src/app/dashboard/_components/StationPicker.tsx, src/lib/format.ts]
autonomous: true

must_haves:
  truths:
    - "Rankings permiten ordenar y buscar estaciones"
    - "El selector de estacion permite elegir por nombre"
    - "Textos y estados vacios aparecen en espanol"
  artifacts:
    - path: "src/app/dashboard/_components/RankingsTable.tsx"
      provides: "Tabla sortable y searchable de rankings"
      exports: ["RankingsTable"]
    - path: "src/app/dashboard/_components/StationPicker.tsx"
      provides: "Selector de estacion para patrones/heatmap"
      exports: ["StationPicker"]
  key_links:
    - from: "src/app/dashboard/_components/RankingsTable.tsx"
      to: "src/lib/format.ts"
      via: "formatPercent y etiquetas"
      pattern: "formatPercent"
    - from: "src/app/dashboard/_components/StationPicker.tsx"
      to: "src/app/dashboard/_components/DashboardClient.tsx"
      via: "selectedStationId"
      pattern: "selectedStationId"
---

<objective>
Completar rankings interactivos y un selector de estacion para el resto de paneles.

Purpose: Permitir explorar estaciones por uso y problemas, y elegir estacion para patrones/heatmap.
Output: Tabla de rankings con orden y busqueda, y selector de estacion usable en movil y desktop.
</objective>

<execution_context>
@/home/guichu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/guichu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/_components/DashboardClient.tsx
@src/app/dashboard/_components/RankingsTable.tsx
@src/app/dashboard/_components/StationPicker.tsx
@src/lib/format.ts
@src/analytics/queries/read.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar el selector de estacion en espanol</name>
  <files>src/app/dashboard/_components/StationPicker.tsx</files>
  <action>
Implementa `StationPicker` como Client Component con un input de busqueda y un selector (datalist o select con filtrado) que permita elegir estacion por nombre. Usa `selectedStationId` y `onSelectStation` recibidos por props, muestra placeholder en espanol y mantiene la seleccion visible. Debe funcionar bien en movil y desktop.
  </action>
  <verify>npm run lint</verify>
  <done>El usuario puede buscar y seleccionar una estacion con textos en espanol.</done>
</task>

<task type="auto">
  <name>Task 2: Construir la tabla de rankings con orden y busqueda</name>
  <files>src/app/dashboard/_components/RankingsTable.tsx, src/lib/format.ts</files>
  <action>
Implementa `RankingsTable` para mostrar rankings de "mas usadas" y "mas problematicas" con tabs o toggle. Une `rankings` con el mapa de estaciones para mostrar nombre y capacidad. Agrega busqueda por nombre o id y ordenamiento por columna (score/uso y problemas). Usa etiquetas en espanol y formatea porcentajes con `formatPercent`. Incluye estados vacios claros cuando no hay datos.
  </action>
  <verify>npm run lint</verify>
  <done>La tabla permite filtrar y ordenar rankings, con textos en espanol y datos reales.</done>
</task>

</tasks>

<verification>
- `npm run lint`
</verification>

<success_criteria>
- Selector de estacion funcional y usable en movil.
- Rankings visibles con busqueda y ordenamiento en espanol.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-frontend/05-04-SUMMARY.md`
</output>
